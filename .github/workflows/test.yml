---
name: Test

on: pull_request

env:
  DEBUG: ${{ secrets.ACTIONS_STEP_DEBUG }}
  MISE_TRACE: ${{ secrets.ACTIONS_STEP_DEBUG && 1 || 0 }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v2
      - run: mise run test
